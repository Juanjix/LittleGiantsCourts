---
// Props opcionales para personalizar
const { titulo = "La primer mini cancha de paddle pensada para chicos", subtitulo = "Portátil, segura y creada para divertirse en familia", imagen = '', buttonText = 'WhatsApp', buttonText2 = 'Contacto', whatsapp = '' } = Astro.props;
---

<style>
  section{
    position: relative;
    overflow: hidden;
    background: ;
    width: 100%;
    height: 100vh;
  }
  
  h1{
    color: white;
  }
</style>

<section class=`bg-secondary  relative w-screen h-screen overflow-hidden bg-[url(images/lgc-background.webp)] bg-cover bg-center` >
  
  <!-- Capa oscura encima para mejorar la lectura -->
  <div class="absolute inset-0 bg-black/50"></div>

  <!-- Contenido -->
  <div class="relative z-10 flex flex-col justify-center h-full max-w-7xl px-8 mx-auto text-white contenido">
    <div class="max-w-lg">
      <h1 class="text-4xl md:text-6xl font-bold mb-4">{titulo}</h1>
      <p class="text-lg md:text-xl mb-8">{subtitulo}</p>
      <div class="flex gap-4">
        <a 
          href=`https://wa.me/${whatsapp}` 
          target="_blank" 
          class="px-6 py-3 bg-green-500 hover:bg-green-600 rounded-lg font-semibold transition"
        >
          {buttonText}
        </a>
        <a 
          href="#otra-seccion" 
          class="px-6 py-3 bg-transparent text-white border hover:border-secondary rounded-lg font-semibold transition"
        >
          {buttonText2}
        </a>
      </div>
    </div>
  </div>
</section>
